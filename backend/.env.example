NOTION_TOKEN=secret_your_notion_integration_token
NOTION_DATABASE_ID=your_notion_database_id

# Optional. Defaults to allowing all origins, which is convenient while the
# Chrome extension ID changes during local development.
ALLOWED_EXTENSION_ORIGIN=*
