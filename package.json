{
  "name": "link-snagged-in",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for extracting LinkedIn job post details for Notion.",
  "type": "module",
  "packageManager": "bun@1.3.5",
  "scripts": {
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "build": "bun scripts/build.mjs",
    "verify": "bun run typecheck && bun run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "esbuild": "^0.25.12",
    "typescript": "^5.9.3"
  }
}
